# Source clips

Drop your source video files here (`.mp4`, `.mov`, `.mkv`, `.webm`, ...).

The pipeline probes every clip in this folder with `ffprobe` and gives Claude
their durations and resolutions so it can decide which segments to cut. Clips
are the only footage the editor is allowed to use.
