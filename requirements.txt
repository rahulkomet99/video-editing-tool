# Runtime dependencies, pinned for reproducible installs.
anthropic==0.115.0
pydantic==2.13.4
PyYAML==6.0.3
python-dotenv==1.2.2
httpx==0.28.1
feedparser==6.0.12
streamlit==1.58.0
pillow==12.2.0          # exact caption text measurement (auto-fit)
# HTTP job API (src/service).
fastapi==0.139.0
uvicorn[standard]==0.49.0
# Auto word-by-word captions (speech-to-text). Pulls ctranslate2/av; CPU-only
# by default. Optional at runtime — the pipeline degrades gracefully without it.
faster-whisper==1.2.1
